import { BelowFoldSections } from "./LightSections";

/** Lazy chunk: same tree as `BelowFoldSections` (see `Light.tsx` sync path for Node SSR). */
export default function LightBelowFold() {
  return <BelowFoldSections />;
}
